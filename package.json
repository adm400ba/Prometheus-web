{
  "name": "prometheus-web-obfuscator",
  "version": "1.0.0",
  "description": "Interface Web para Prometheus Lua Obfuscator",
  "main": "server.js",
  "scripts": {
    "start": "curl -fsSL https://raw.githubusercontent.com/prometheus-lua/Prometheus/master/install.sh | sh && node server.js",
    "build": "echo 'Build concluido'"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
